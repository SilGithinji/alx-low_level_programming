#include <stdio.h>
/**
 * main - Entry point
 * Description: 'print text and start new line'
 * Return: Always 0 (Success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
