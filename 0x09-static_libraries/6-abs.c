#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @t: The integer being computed
 * Return: Absolute value
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
